# ARAD: Gunblade — 던전앤파이터 팬 게임

던전앤파이터의 **총검사(Gunblade)**를 주인공으로 한 2.5D 픽셀아트 로그라이크 핵앤슬래시 팬 게임입니다.
Three.js로 제작되었으며, 브라우저에서 바로 플레이할 수 있습니다.

> ⚠️ 본 프로젝트는 비영리 **팬 게임**입니다. Dungeon &amp; Fighter는 NEOPLE/NEXON의 지적 재산입니다.

## 🎮 게임 소개

리볼버와 카타나를 함께 다루는 총검사가 되어, 마을에서 장비와 특성을 갖추고
무작위로 생성되는 던전을 돌파합니다. 방을 클리어할 때마다 특성을 획득해
점점 더 강력한 빌드로 보스에게 도전하세요.

- **마을 ↔ 던전** — 마을에서 상점·회복·특성 시설을 이용하고 포탈로 던전에 입장
- **총검 이중 무기** — 좌클릭 리볼버 연사(탄창제 + 재장전) + 우클릭/Space 카타나 광역 베기
- **절차적 던전** — 방들이 격자 위의 연결 그래프로 생성되어 매 런마다 구조가 달라짐
- **방 종류** — 전투 / 엘리트(특성 보상) / 보물 / 상점 / 보스
- **로그라이크 성장** — 시작 특성 선택 + 방 클리어마다 3개 중 1개 특성 강화
  (희귀도: Common / Rare / Epic / Legendary)
- **골드 경제** — 적 처치·상자·엘리트 보상으로 골드를 모아 마을 상점에서 소비
- **다양한 적** — 임프(근접 고블린) · 슈터(화염 고블린, 원거리) · 브루트(타우 전사) · 보스(타우 족장)

## ⌨️ 조작법

| 키 | 동작 |
| --- | --- |
| `W` `A` `S` `D` | 이동 |
| `마우스` | 조준 |
| `좌클릭` (홀드) | 리볼버 연사 |
| `우클릭` / `Space` | 카타나 베기 (광역 + 넉백) |
| `R` | 재장전 |
| `Shift` | 대시 회피 (짧은 무적) |
| `E` | 상호작용 (상점·분수·제단·상자·문·포탈) |

## 🕹️ 실행 방법

```bash
npm install
npm run dev      # 개발 서버 (http://localhost:5173)
```

프로덕션 빌드:

```bash
npm run build    # tsc 타입 체크 + dist/ 생성
npm run preview  # 빌드 결과 미리보기
```

시각적 회귀 검증(빌드 → 헤드리스 플레이 → 스크린샷):

```bash
npm run qc       # qc-out/ 에 contact.png, 단계별 스크린샷, report.txt 생성
```

`npm run build`는 타입만 검사하므로 스프라이트 크기/앵커/이펙트 배치 같은
시각적 버그는 잡지 못합니다. 변경 후에는 항상 `npm run qc`로 `qc-out/contact.png`를
직접 확인합니다.

## 🏘️ 마을 시설

| 시설 | 기능 |
| --- | --- |
| 던전 포탈 | 현재 스테이지 던전 입장 |
| 상인 | 골드로 소비 아이템 구매 |
| 회복 분수 | 체력 회복 |
| 시작 특성 제단 | 런 시작 시 첫 특성 선택 (런당 1회) |
| 특성 제련소 | 보유 특성 중 하나를 강화 (런당 1회) |

## 📁 프로젝트 구조

```
src/
├── main.ts                  # 진입점, window.__game QC 훅
├── config.ts                 # 게임 밸런스 / 색상 팔레트
├── core/
│   ├── Game.ts               # 게임 루프 · 상태 · 마을/던전 흐름 (오케스트레이터)
│   └── Input.ts               # 키보드 / 마우스 입력
├── entities/
│   ├── Player.ts               # 총검사 플레이어 (이동·조준·전투·성장)
│   ├── CharacterSprite.ts      # 플레이어 스프라이트 애니메이션 (idle/walk/dash/katana/pistol)
│   ├── Enemy.ts                # 적 AI (임프·슈터·브루트·보스) + 엘리트 변형
│   ├── EnemySprite.ts          # 적 스프라이트 시트 애니메이션
│   ├── Interactable.ts         # 상호작용 오브젝트 (상점/분수/제단/제련소/상자/문/포탈)
│   └── models.ts
├── systems/
│   ├── RunState.ts             # 절차적 던전 방 그래프 생성 · 진행 상태
│   ├── Room.ts                 # 방 경계 · 출입구 좌표
│   ├── Shop.ts                 # 골드 상점
│   ├── Upgrades.ts             # 로그라이크 특성 풀 (희귀도별)
│   ├── Pickups.ts              # 경험치/골드 오브 획득
│   ├── Projectiles.ts          # 총알 / 적 투사체
│   ├── Effects.ts               # 파티클·베기 궤적·사망 연출
│   └── Audio.ts
├── rendering/
│   ├── assets.ts                # 픽셀 애셋 경로/로더
│   ├── tiles.ts                 # 바닥/벽 타일링
│   ├── toon.ts                  # 셀 셰이딩 아웃라인
│   └── pixelfx.ts
└── ui/
    └── HUD.ts                    # HP/XP·골드·미니맵·상호작용 프롬프트·레벨업·상점 UI
```

## 🎨 아트 & 기술 스택

- 픽셀아트 스프라이트 시트 기반 2.5D 빌보드 렌더링 (Three.js 직교 카메라)
- 플레이어/적 스프라이트는 상태별(대기·이동·공격) 가로 스트립 시트, 발이 셀 하단에 정렬
- [Three.js](https://threejs.org/) — 렌더링
- TypeScript + Vite
- Playwright 기반 헤드리스 시각 회귀 검증 (`tools/qc.mjs`)
