# ARAD: Gunblade 아트·화면 제작 규격

이 문서는 새 에셋을 만들거나 기존 에셋을 교체할 때 화면 비율, 픽셀 밀도,
애니메이션 기준점이 다시 흔들리지 않게 하기 위한 공통 규격이다.

## 1. 화면 기준

- 게임 내부 기준 해상도는 **1920×1080, 16:9**다.
- 브라우저가 더 크거나 작아도 내부 좌표는 바뀌지 않는다. 전체 게임 화면을
  같은 비율로 확대·축소하며, 남는 영역은 검은 여백으로 처리한다.
- HUD는 화면 가장자리에서 최소 24px 이상 떨어뜨린다.
- 중요한 안내, 보스 체력, 선택 창은 화면 중앙 80% 안에 둔다.
- 실제 픽셀 아트에는 브라우저 보간을 적용하지 않는다. 코드에서는
  `NearestFilter`, `image-rendering: pixelated`, mipmap 비활성화를 유지한다.
- Three.js 텍스처·캔버스 텍스처·하단 기준 스프라이트를 만들 때는
  `src/rendering/pixelArt.ts`의 공통 함수를 사용한다.

## 2. 공통 스프라이트 규칙

- PNG, RGBA, 투명 배경으로 내보낸다.
- 스프라이트 시트는 **가로 한 줄**, 모든 프레임은 같은 셀 크기를 사용한다.
- 셀의 기준점은 **하단 중앙**이다. 각 프레임의 발 또는 바닥 접점은 셀 하단에서
  0~2px 안에 위치해야 한다.
- 첫 프레임과 마지막 프레임의 네 모서리는 완전히 투명해야 한다.
- 안티앨리어싱, 반투명 외곽선, 리사이즈 보간, 흐림 효과를 사용하지 않는다.
- 같은 종류의 캐릭터는 상태가 바뀌어도 머리·몸통의 픽셀 크기가 변하지 않아야
  한다. 공격 동작 때문에 자세가 낮아지는 것은 허용하지만 몸 자체를 축소하지 않는다.
- 게임에서 크기를 맞추기 위해 원본 PNG를 늘이거나 찌그러뜨리지 않는다.
  보이는 크기는 코드의 단일 `scale` 값으로만 조절한다.

## 3. 캐릭터

현재 확정 시트는 `public/gunblader.png`다.

- 프레임 셀: **112×64px**
- 총 프레임: **27**
- 시트 전체: **3024×64px**
- 순서:
  - 대기 0~3
  - 걷기 4~10
  - 검 공격 11~18
  - 총 공격 19~26
- 캐릭터 외형과 무기는 한 프레임 안에서 같은 기준점에 맞춘다.
- 향후 파츠 분리형 시트로 교체할 때도 모든 레이어는 112×64px 셀과 동일한
  발 기준선을 공유해야 한다. 별도 파츠의 빈 공간도 삭제하지 않는다.

## 4. 몬스터

몬스터는 체급에 따라 셀 크기를 다르게 쓸 수 있다.

- 소형: 48×48px 권장
- 중형: 64×64px 권장
- 대형·보스: 96×96px 이상 가능
- 대기·걷기·공격 시트는 같은 몬스터 안에서 동일한 셀 크기와 픽셀 밀도를 쓴다.
- 보이는 체급 차이는 원본을 비균등 확대하지 않고 `EnemySprite.ts`의 단일 스케일로
  조정한다.
- 알려진 기존 몬스터 원화 결함은 코드에서 보정하지 않는다. 새 시트를 같은 파일
  규격으로 교체한다.

## 5. 오브젝트와 이펙트

- 오브젝트는 실제 가로세로 비율을 유지한다. 코드의 `ASPECT` 값과 PNG 비율이
  2% 이상 다르면 안 된다.
- 지면에 놓이는 오브젝트는 그림자 접점을 하단 중앙에 둔다.
- 공격 이펙트는 캐릭터 스프라이트 안에 합치지 않고 별도 투명 시트로 만든다.
- 방향성이 있는 이펙트는 기본 방향을 **오른쪽(+X)** 으로 제작한다.
- 이펙트 시트도 프레임별 크기와 중심이 흔들리지 않게 고정한다.

## 6. UI 에셋

- UI 아이콘은 32px 또는 64px 정사각형 원본을 권장한다.
- 선 두께와 픽셀 크기를 같은 세트 안에서 통일한다.
- 색만으로 상태를 구분하지 않는다. 모양, 외곽선, 기호 중 하나를 함께 바꾼다.
- HUD 텍스트가 포함된 이미지는 만들지 않는다. 텍스트는 HTML UI에서 표시한다.

## 7. 납품 전 확인

1. PNG가 RGBA 투명 배경인가?
2. 모든 프레임 셀 크기가 같은가?
3. 발 또는 바닥 접점이 하단 중앙에 고정됐는가?
4. 상태 전환 중 몸의 픽셀 밀도와 크기가 유지되는가?
5. 무기와 이펙트의 기본 방향이 오른쪽(+X)인가?
6. 파일명과 프레임 수가 코드 선언과 일치하는가?
7. `npm run qc`가 통과하고 `qc-out/contact.png`에서 직접 확인했는가?
